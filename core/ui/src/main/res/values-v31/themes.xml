<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- API 31+: suppress Shellify branding on the system splash for WebViewActivity.
         The per-PWA in-app overlay (showSplash) provides the branded loading experience. -->
    <style name="Theme.Shellify.WebView" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/black</item>
    </style>

</resources>
